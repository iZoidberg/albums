<%= semantic_form_for @album do |f| %>
		<%= f.inputs do %>
				<%= f.input :title %>
				<%= f.input :artist %>
		<% end %>
		<%= f.actions do %>
				<%= f.action :submit, :as => :button %>
		<% end %>
<% end %>
